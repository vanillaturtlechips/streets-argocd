apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: nginx-member-hpa
  namespace: nginx-member
  labels:
    app: nginx-member
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: nginx-member-deploy
  
  minReplicas: 2
  maxReplicas: 5

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
    
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 40

    - type: Pods
      pods:
        metric:
          name: http_requests_per_second 
        target:
          type: AverageValue
          averageValue: 50
