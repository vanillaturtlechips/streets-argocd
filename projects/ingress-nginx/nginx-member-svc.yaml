apiVersion: v1
kind: Service
metadata:
  name: nginx-member-svc
  namespace: ingress-nginx
  labels:
    app: nginx-member
spec:
  selector:
    app: nginx-member
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80